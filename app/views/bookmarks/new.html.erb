
<%= simple_form_for @bookmark, url: polymorphic_path([:list, @bookmark]) do |f| %>
  <%= f.input :comment%>
  <%= f.association :list %>
  <%= f.association :movie %>
  <%= f.button :submit %>
<% end %>
